<div class="timeLine section">
<h2><%= @user.profile.name %>'s TimeLine</h2>
</div>
<% sorted_posts = @user.sort_posts %>
    <% sorted_posts.each do |post| %> 
        <%= render partial: 'posts/post', locals: {post: post, user: @user} %>
    <% end %>

